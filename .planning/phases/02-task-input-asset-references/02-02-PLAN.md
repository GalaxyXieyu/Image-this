---
phase: 02-task-input-asset-references
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/hooks/useImageProcessing.ts
  - src/app/workspace/page.tsx
  - src/app/api/tasks/route.ts
autonomous: true
requirements:
  - ASSET-03
  - ASSET-04
user_setup: []
must_haves:
  truths:
    - 前端提交任务时不再直接把大 base64 塞进任务队列
    - 任务 `inputData` 体积明显变轻
  artifacts:
    - 前端任务创建入口改造
    - 任务创建接口支持资产引用输入
  key_links:
    - 前端输入资产保存与任务创建之间有稳定衔接
---

<objective>
把前端任务创建链路从 base64 直接入队，改成“先保存输入资产，再传引用”。

Purpose: 这是去 base64 化最直接的一步，能立刻降低请求体积和队列负担。
Output: 工作区、视频任务和批量任务创建链路全面切换到资产引用化。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-input-asset-references/02-CONTEXT.md
@src/hooks/useImageProcessing.ts
@src/app/workspace/page.tsx
@src/app/api/tasks/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造批量图片任务创建链路</name>
  <files>src/hooks/useImageProcessing.ts</files>
  <read_first>src/hooks/useImageProcessing.ts, src/lib/storage.ts</read_first>
  <action>把 `handleExpansion` / `handleUpscaling` / `handleOneClick` / `handleBackgroundReplace` / `handleWatermark` 的输入创建流程改为先落地输入资产，再传轻量引用。要求过渡期兼容旧字段，但新任务默认走引用。</action>
  <verify>用 `rg -n "readAsDataURL|imageUrl: resized|inputAsset|referenceAsset" src/hooks/useImageProcessing.ts` 检查链路变化</verify>
  <acceptance_criteria>
    - 批量任务创建默认不再把原图 base64 直接塞进任务 payload
    - 参考图也走资产引用
  </acceptance_criteria>
  <done>批量任务前端创建链路完成引用化</done>
</task>

<task type="auto">
  <name>Task 2: 改造视频任务创建链路</name>
  <files>src/app/workspace/page.tsx</files>
  <read_first>src/app/workspace/page.tsx, src/lib/storage.ts</read_first>
  <action>替换视频任务当前 `FileReader.readAsDataURL()` 直传逻辑，改为保存输入资产后只把引用塞进任务。保留用户提示和 worker 触发逻辑，但任务 payload 必须明显变轻。</action>
  <verify>用 `rg -n "readAsDataURL|imageBase64|inputAsset" src/app/workspace/page.tsx` 检查视频链路</verify>
  <acceptance_criteria>
    - 视频任务创建不再依赖前端直接传整张图片 base64
    - 用户侧行为不回退
  </acceptance_criteria>
  <done>视频任务链路完成引用化</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 新任务 `inputData` 体积相比旧方案明显下降
- [ ] 批量任务和视频任务都能创建成功
- [ ] worker 触发、失败提示和历史页不因输入引用化而回退
</verification>

<success_criteria>

- 前端主要任务创建链路完成去 base64 化
- 任务队列负担明显下降
- 用户体验与现有处理流程保持一致
  </success_criteria>

<output>
After completion, create `.planning/phases/02-task-input-asset-references/02-02-SUMMARY.md`
</output>
