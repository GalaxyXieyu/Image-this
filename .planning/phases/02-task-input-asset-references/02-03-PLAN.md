---
phase: 02-task-input-asset-references
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/app/api/tasks/worker/route.ts
  - src/app/api/tasks/retry/route.ts
  - src/app/api/tasks/recover/route.ts
  - src/app/history/page.tsx
autonomous: true
requirements:
  - ASSET-05
  - ASSET-06
user_setup: []
must_haves:
  truths:
    - worker 从输入资产引用读取文件，按需在 provider 边界转 base64
    - 老任务和重试/恢复路径在迁移期仍可工作
  artifacts:
    - worker 输入读取兼容层
    - 重试/恢复兼容逻辑
  key_links:
    - 新旧任务输入模型在 worker 和历史链路中都可被解析
---

<objective>
让 worker 真正消费输入资产引用，并在迁移期兼容旧任务。

Purpose: 只有 worker 改完，这个 phase 才真正闭环，否则前端只是换了传参方式，执行层仍旧脆弱。
Output: worker 输入兼容层、provider 边界按需 base64 转换，以及重试/恢复/历史兼容处理。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-input-asset-references/02-CONTEXT.md
@src/app/api/tasks/worker/route.ts
@src/app/api/tasks/retry/route.ts
@src/app/api/tasks/recover/route.ts
@src/app/history/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造 worker 输入读取与 provider 边界转换</name>
  <files>src/app/api/tasks/worker/route.ts</files>
  <read_first>src/app/api/tasks/worker/route.ts, src/lib/local-storage.ts, src/lib/storage.ts</read_first>
  <action>让 worker 优先读取 `inputAsset` / `referenceAsset`，在 provider 真正需要图片内容时再从本地文件读取，并按需转为 base64。迁移期保留对旧 `imageUrl` 字段的回退支持。</action>
  <verify>用 `rg -n "inputAsset|referenceAsset|base64|readFile|imageUrl" src/app/api/tasks/worker/route.ts` 检查兼容层</verify>
  <acceptance_criteria>
    - worker 可消费新任务输入资产引用
    - provider 边界外不再长期携带 base64
    - 旧任务仍能运行
  </acceptance_criteria>
  <done>worker 完成输入引用化闭环</done>
</task>

<task type="auto">
  <name>Task 2: 补齐重试/恢复/历史兼容</name>
  <files>src/app/api/tasks/retry/route.ts, src/app/api/tasks/recover/route.ts, src/app/history/page.tsx</files>
  <read_first>src/app/api/tasks/retry/route.ts, src/app/api/tasks/recover/route.ts, src/app/history/page.tsx</read_first>
  <action>确保重试任务会保留新输入资产引用；恢复逻辑不会因输入模型变化而失效；历史页获取原图和结果图时能够同时兼容旧 `inputData.imageUrl` 和新 `inputAsset` 模型。</action>
  <verify>用 `rg -n "inputAsset|referenceAsset|imageUrl|retry|recover" src/app/api/tasks/retry/route.ts src/app/api/tasks/recover/route.ts src/app/history/page.tsx` 检查兼容处理</verify>
  <acceptance_criteria>
    - 重试和恢复链路兼容新旧任务
    - 历史页不因输入引用化而丢失原图展示
  </acceptance_criteria>
  <done>迁移兼容链路齐全</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] worker 新旧任务都能执行
- [ ] 重试和恢复链路不回退
- [ ] 历史页/任务中心能继续查看结果与原图
</verification>

<success_criteria>

- 去 base64 化闭环打通到 worker 层
- 迁移期兼容策略明确且可运行
- 后续可再考虑清理旧任务模型和历史 base64 遗留
  </success_criteria>

<output>
After completion, create `.planning/phases/02-task-input-asset-references/02-03-SUMMARY.md`
</output>
