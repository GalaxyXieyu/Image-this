---
phase: 02-task-input-asset-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/storage.ts
  - src/lib/local-storage.ts
  - src/lib/user-config.ts
  - src/app/api/tasks/route.ts
autonomous: true
requirements:
  - ASSET-01
  - ASSET-02
user_setup: []
must_haves:
  truths:
    - 前端在创建任务前可以把输入图片落为稳定的本地输入资产引用
    - 任务创建接口可接受轻量输入资产引用
  artifacts:
    - 输入资产保存能力
    - 面向任务创建的输入资产引用结构
  key_links:
    - 任务创建链路与本地存储层之间存在清晰的 asset 引用契约
---

<objective>
建立输入资产落地能力，让任务创建从“直接塞 base64”过渡到“先保存输入资产，再传引用”。

Purpose: 这是去 base64 化的基础，没有输入资产引用，后续 worker 和 provider 侧都无法脱离大 payload。
Output: 输入资产保存与引用协议的第一版实现。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-input-asset-references/02-CONTEXT.md
@src/lib/storage.ts
@src/lib/local-storage.ts
@src/app/api/tasks/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 设计输入资产引用结构</name>
  <files>src/app/api/tasks/route.ts, src/lib/storage.ts</files>
  <read_first>src/app/api/tasks/route.ts, src/lib/storage.ts, src/lib/local-storage.ts</read_first>
  <action>定义任务输入资产引用结构，例如 `inputAsset`, `referenceAsset`，至少包含本地可读路径、展示用 URL 或 assetId。要求这个结构能支撑 worker 重试和恢复，不依赖前端内存对象。</action>
  <verify>用 `rg -n "inputAsset|referenceAsset|assetId|assetPath" src/app/api/tasks/route.ts src/lib/storage.ts` 检查引用结构落地</verify>
  <acceptance_criteria>
    - 存在明确的输入资产引用字段定义
    - 任务创建接口不再被迫要求图片 base64 作为唯一输入
  </acceptance_criteria>
  <done>输入资产引用协议明确</done>
</task>

<task type="auto">
  <name>Task 2: 实现输入资产落地能力</name>
  <files>src/lib/storage.ts, src/lib/local-storage.ts</files>
  <read_first>src/lib/storage.ts, src/lib/local-storage.ts, src/lib/user-config.ts</read_first>
  <action>实现“输入资产保存”能力，与结果图保存解耦。需要考虑默认目录、用户自定义路径、文件名安全和后续 worker 可读性。优先让桌面端本地文件路径稳定可复用。</action>
  <verify>用 `rg -n "input asset|asset reference|uploadInput|saveInput" src/lib/storage.ts src/lib/local-storage.ts` 检查实现点</verify>
  <acceptance_criteria>
    - 输入资产可保存到本地
    - 输入资产路径或标识可安全回传给任务链路
  </acceptance_criteria>
  <done>输入资产可以稳定落地</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 输入资产保存能力有明确 API 或函数入口
- [ ] 资产引用结构能支撑后续 worker 读取
- [ ] 不破坏现有结果图保存路径
</verification>

<success_criteria>

- 输入资产引用结构成立
- 本地输入资产可稳定持久化
- 为后续任务 payload 瘦身做好基础设施准备
  </success_criteria>

<output>
After completion, create `.planning/phases/02-task-input-asset-references/02-01-SUMMARY.md`
</output>
