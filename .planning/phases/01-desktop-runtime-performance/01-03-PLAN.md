---
phase: 01-desktop-runtime-performance
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/lib/prisma.ts
  - electron/database-manager.js
  - electron/main.js
  - src/lib/storage.ts
  - src/lib/local-storage.ts
autonomous: true
requirements:
  - PERF-03
  - PERF-06
  - PERF-04
user_setup: []
must_haves:
  truths:
    - Windows 本地运行路径具备明确的 SQLite 与文件 IO 调优
    - 桌面启动链路中的非关键阻塞被压缩或延后
    - Windows 安装、重装和自动更新不会默认删除既有数据库和用户配置
  artifacts:
    - SQLite 调优代码或初始化逻辑
    - 启动链路和日志 IO 的优化改动
    - 安装/更新脚本的数据保留修复
  key_links:
    - Prisma/SQLite 初始化、Electron 启动、storage 实现之间的调优策略一致
---

<objective>
针对 Windows 本地运行环境做数据库、文件 IO、安装更新数据安全和启动链路调优，降低接口和启动体感延迟并确保不会删库。

Purpose: 在后台 worker 和轻量接口就位后，Windows 端仍会受到 SQLite、日志写盘、本地文件操作和安装更新链路的拖累，需要专门治理。
Output: SQLite PRAGMA 调优、启动 warmup 减阻、文件 IO/配置读取减负，以及安装更新保留用户数据的修复方案。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-desktop-runtime-performance/01-CONTEXT.md
@src/lib/prisma.ts
@electron/database-manager.js
@electron/main.js
@src/lib/storage.ts
@src/lib/local-storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为桌面 SQLite 初始化增加调优策略</name>
  <files>src/lib/prisma.ts, electron/database-manager.js</files>
  <read_first>src/lib/prisma.ts, electron/database-manager.js, prisma/schema.prisma</read_first>
  <action>在桌面运行场景为 SQLite 增加明确的 PRAGMA 调优和初始化策略，优先考虑 `journal_mode=WAL`、`busy_timeout`、`synchronous=NORMAL` 等；确认调优落点既能覆盖 Prisma 查询，也不会破坏桌面模板库和迁移逻辑。</action>
  <verify>用 `rg -n "PRAGMA|journal_mode|busy_timeout|synchronous|WAL" src/lib/prisma.ts electron/database-manager.js` 检查调优落地</verify>
  <acceptance_criteria>
    - 桌面 SQLite 有明确调优代码
    - 调优逻辑与现有数据库准备流程兼容
  </acceptance_criteria>
  <done>数据库层具备 Windows 桌面专项调优</done>
</task>

<task type="auto">
  <name>Task 2: 降低启动链路与本地文件 IO 的同步阻塞</name>
  <files>electron/main.js, src/lib/storage.ts, src/lib/local-storage.ts, build/installer.nsh, electron/app-runtime.js</files>
  <read_first>electron/main.js, src/lib/storage.ts, src/lib/local-storage.ts, build/installer.nsh, electron/app-runtime.js</read_first>
  <action>压缩 `bootstrapApplication()` 的串行阻塞路径，延后非关键 warmup；减少同步日志或高频重复配置读取，优先治理 `getUserConfig()` 的重复调用与主进程同步写日志对 Windows 的影响。同时修复 NSIS 安装脚本，禁止在默认安装、重装和自动更新中清理 `userData` 目录，把“清空本地数据”改成显式人工选择或完全移除。</action>
  <verify>用 `rg -n "appendFileSync|warmupApplication|getUserConfig|writeFile|mkdir|CleanImagineThisUserData|customInstall|customUnInstall" electron/main.js src/lib/storage.ts src/lib/local-storage.ts build/installer.nsh` 检查优化点</verify>
  <acceptance_criteria>
    - 启动主链路减少非关键阻塞
    - 高频文件 IO 或配置读取路径得到收敛
    - 安装/更新默认行为不会清理用户数据库和配置
  </acceptance_criteria>
  <done>Windows 端启动和本地 IO 压力下降，且安装更新不再默认删库</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SQLite 初始化链路包含明确的桌面调优配置
- [ ] Electron 启动日志能区分关键启动步骤与延后 warmup
- [ ] 高频存储调用路径减少重复读取用户配置或不必要的同步 IO
- [ ] 安装版重装/更新不会默认删除 `app.getPath('userData')` 下的数据
- [ ] Windows 实机按 `01-WINDOWS-REGRESSION.md` 完成 Case 1 / Case 2 / Case 3 / Case 4 / Case 8
</verification>

<success_criteria>

- Windows 端数据库和本地文件访问具备针对性优化
- 启动阶段的主线程阻塞被控制
- 安装和自动更新路径具备用户数据保留保证
- 相关优化具备代码级证据和验证路径
  </success_criteria>

<output>
After completion, create `.planning/phases/01-desktop-runtime-performance/01-03-SUMMARY.md`
</output>
