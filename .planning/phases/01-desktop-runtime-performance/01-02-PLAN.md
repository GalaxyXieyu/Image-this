---
phase: 01-desktop-runtime-performance
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/app/api/tasks/route.ts
  - src/hooks/useTaskPolling.ts
  - src/components/navigation/FloatingTaskButton.tsx
  - src/app/history/page.tsx
autonomous: true
requirements:
  - PERF-02
  - PERF-04
user_setup: []
must_haves:
  truths:
    - 任务状态轮询只获取轻量状态字段
    - 任务历史和任务状态查询各自承担独立职责
  artifacts:
    - 新的轻量状态接口或等价查询分流逻辑
    - 轮询相关前端调用改为使用轻量接口
  key_links:
    - 轮询 hook、浮动任务按钮和任务 API 的字段契约一致
---

<objective>
拆分任务列表接口与状态轮询接口，减少任务查询的返回体积和数据库负载。

Purpose: 当前最直接的慢点来自 `/api/tasks` 同时承担列表页、轮询、统计与大字段返回，这需要先拆职责。
Output: 一个轻量任务状态查询链路，以及前端轮询调用的对齐改造。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-desktop-runtime-performance/01-CONTEXT.md
@src/app/api/tasks/route.ts
@src/hooks/useTaskPolling.ts
@src/components/navigation/FloatingTaskButton.tsx
@src/app/history/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 拆分轻量状态查询接口</name>
  <files>src/app/api/tasks/route.ts</files>
  <read_first>src/app/api/tasks/route.ts, prisma/schema.prisma</read_first>
  <action>为任务轮询场景设计轻量查询路径。可以新增 `/api/tasks/status` 或在现有 route 中为 `ids` 查询提供专门分支，但必须去掉 `count/groupBy` 与 `inputData/outputData` 大字段返回，只保留状态、进度、当前步骤和必要的结果标识。</action>
  <verify>用 `rg -n "inputData|outputData|groupBy|count|ids" src/app/api/tasks/route.ts src/app/api/tasks` 检查轮询路径是否瘦身</verify>
  <acceptance_criteria>
    - 轮询路径不返回大体积任务字段
    - 轮询路径不执行列表页级别统计查询
  </acceptance_criteria>
  <done>状态轮询接口实现轻量化</done>
</task>

<task type="auto">
  <name>Task 2: 对齐工作区和浮动任务入口的轮询调用</name>
  <files>src/hooks/useTaskPolling.ts, src/components/navigation/FloatingTaskButton.tsx, src/app/history/page.tsx</files>
  <read_first>src/hooks/useTaskPolling.ts, src/components/navigation/FloatingTaskButton.tsx, src/app/history/page.tsx</read_first>
  <action>把工作区轮询和浮动任务按钮改为使用轻量状态链路；历史页保留列表接口，但避免它承担轮询职责。必要时补字段适配，确保旧 UI 不依赖 `inputData/outputData` 才能显示状态。</action>
  <verify>用 `rg -n "/api/tasks" src/hooks/useTaskPolling.ts src/components/navigation/FloatingTaskButton.tsx src/app/history/page.tsx` 检查调用路径</verify>
  <acceptance_criteria>
    - 工作区轮询不再调用重列表接口
    - 浮动任务按钮的状态查询与最近任务列表职责分开
  </acceptance_criteria>
  <done>前端轮询链路完成降载</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 轮询接口返回体积相比原接口明显收缩
- [ ] 工作区轮询、浮动任务按钮和历史页都能正常显示状态
- [ ] 任务历史页仍可查看结果图/视频，不因接口瘦身而回归
</verification>

<success_criteria>

- 轮询和列表职责清晰分离
- 任务状态查询不再承载大 JSON 与统计查询
- 相关 UI 使用新契约后不破坏现有流程
  </success_criteria>

<output>
After completion, create `.planning/phases/01-desktop-runtime-performance/01-02-SUMMARY.md`
</output>
