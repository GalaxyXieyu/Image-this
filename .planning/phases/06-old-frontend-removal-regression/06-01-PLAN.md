# Phase 6 Wave 1: Remove Old Frontend Code

**Goal:** Delete unused code and verify all active flows still work.

**Success Criteria:**
1. No unused files in `src/hooks/`, `src/components/`
2. `npm run build` passes
3. Smoke test script ready for manual verification
4. No orphaned imports

## Tasks

### Task 1: Remove unused hooks
- Delete `src/hooks/useInfiniteScroll.ts` (unused)
- Delete `src/hooks/useUserPreferences.ts` (unused)

### Task 2: Verify component inventory
- All components in `src/components/` are referenced by active pages
- No old workspace components remain

### Task 3: Verify no orphaned imports
- No references to `@/stores/*` (already removed)
- No references to `@/features/*` (already removed)
- No references to deleted hooks

### Task 4: Build verification
- `npm run build` passes
- No new TypeScript errors

### Task 5: Smoke test readiness
- `scripts/smoke-tool-tasks.mjs` is available for manual testing
- Documents what credentials are needed

## Files to delete
- `src/hooks/useInfiniteScroll.ts`
- `src/hooks/useUserPreferences.ts`

## Files to verify
- `src/app/page.tsx` (no old workspace links)
- `src/app/workspace/scene/page.tsx` (is the new page)
- All `src/components/*` files are referenced
